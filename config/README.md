# Config README

This folder stores reference configuration files.

`paths.yaml` is reference-only unless project code explicitly reads it. Do not assume code automatically consumes this file.

During initialization, record local project paths, interpreter paths, and important folder paths here.

Do not store passwords, API keys, or private credentials in this folder.
